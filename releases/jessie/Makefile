# vim: noexpandtab:ts=4:sw=4

provider = coldrye
os = debian
release = jessie
feature = apt-cacher-ng
feature_version = 0.8.0
tag := $(shell date +"%Y%m%d%H%M%S")
image = $(os)-$(feature)

include ../../Makefile.in

